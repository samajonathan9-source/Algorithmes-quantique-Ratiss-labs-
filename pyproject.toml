[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "algorithmes-quantique-ratiss-labs"
version = "0.1.0"
description = "Reproducible Grover and RATISS logical-sidecar experiment."
requires-python = ">=3.11"
dependencies = ["qiskit==2.5.2", "qiskit-aer==0.17.2"]
